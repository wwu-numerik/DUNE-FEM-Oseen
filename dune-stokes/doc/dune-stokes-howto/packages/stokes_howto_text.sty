% ----------------------------------------------------------------------------
% -Metapaket                                                                 -
% -felix.albrecht@uni-muenster.de                                            -
% ----------------------------------------------------------------------------

	% 8 statt 7 Bit
\usepackage[T1]{fontenc}
	% Unterstützung für Umlaute
\usepackage[latin1]{inputenc}
	% Unterstützung für Deutsch, kann im Dokument mit \selectlanguage{language}
	% geändert werden
\usepackage[ngerman]{babel}
	% deutsche Behandlung von Absätzen
\usepackage{parskip}
	% Indexgenerierung
\usepackage{makeidx}

\usepackage{textcomp}
	% Nummerierungstiefe der Überschriften
%\setcounter{secnumdepth}{3}
	% Anzeigetiefe der Überschriften im Inhaltsverzeichnis
%\setcounter{tocdepth}{4}
	% Zusätzliche Zeilenlänge im Notfall
%\setlength{\emergencystretch}{20pt}

%---Schriften und Symbole---(für Vektorgraphik)
\usepackage{pifont}		%liefert postscript symbole, z.B. die dingbats
\usepackage{mathpazo}	%mathptmx Ändert mathe auf times (mathpazo auf palatino)
\usepackage{charter}	%Ändert \textrm auf bitstream charter
\usepackage{helvet}		%Ändert \textsf auf Helvetica
%\usepackage{courier}	%Ändert \texttt auf courier

%---Tabellenerweiterung---
\usepackage{array}

%---Einstellungen---
\usepackage{parskip}		%verhindert Einzug bei neuem Absatz
\setcounter{secnumdepth}{3} %Nummerierungstiefe der Überschriften
\setcounter{tocdepth}{4}	%Tiefe, die ins Inhaltsverzeichnis eingeht
\tolerance=2000				%Toleranz für WortzwischenrÃ¤ume
\setlength{\emergencystretch}{20pt} %zusÃ¤tzliche Zeilendehnbarkeit
