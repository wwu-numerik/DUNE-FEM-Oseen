-------------------------------------------------------------
INTRODUCTION TO DUNE-FEM (FEM-HOWTO)
-------------------------------------------------------------

If you just want to read the document, you don't need to completly compile the hole package. 
Checkout the package, go to the subdirectory "doc" and type "make" to create the document.

But if you want to compile the sources, you need to perform the following steps:

I) The following DUNE Modules are required:
    DUNE commom
    DUNE grid
    DUNE fem

II) Checkout the "dune-femhowto" package into your DUNE directory:
       svn checkout svn+ssh://morgoth/raid5/dune/src/svn-archive/dune-femhowto

III) Compile the sources:
   Run dunecontrol with
       ./dune-common/bin/dunecontrol --(your normal config options)  --module=dune-femhowto all

   If you have your config options written in a file (e.g. config.opts) then run
       ./dune-common/bin/dunecontrol --opts=config.opts --module=dune-femhowto all

